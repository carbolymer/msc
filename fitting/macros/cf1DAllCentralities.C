#include <iostream>


cf1DAllCentralities() {
  TCanvas 
    pipiCanvas = new TCanvas("pipi","pipi",800,800),
    kkCanvas = new TCanvas("kk","kk",800,800),
    ppCanvas = new TCanvas("pp","pp",800,800);

  TFile *file = new TFile(merger.GetOutputFileName());

  return 0;
}